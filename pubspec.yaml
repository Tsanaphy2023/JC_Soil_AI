name: soil_app
description: "Soil Parameter Detector - 8-in-1 USB OTG Modbus RTU Agricultural Soil Analyzer"
publish_to: 'none'
version: 1.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  usb_serial: ^0.5.2
  intl: ^0.19.0
  path_provider: ^2.1.3
  share_plus: ^9.0.0
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

